// adding paragraph element before button using the before method.
$("button").click(function(){
    $("button").before("<p>This paragraph is added using before method of jQuery.<br> The paragraph element is added before button.</p>");
});
